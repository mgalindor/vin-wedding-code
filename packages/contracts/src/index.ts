/**
 * Placeholder source file.
 * The actual DTOs and ID types are created in ARC-005.
 * This file exists so that `tsc --noEmit` finds at least one input and
 * the typecheck step in `pnpm typecheck` passes during the bootstrap phase.
 */
export const PLACEHOLDER = true;
