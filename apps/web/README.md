# @wendy/web

Vite + React Web tier skeleton. **This workspace is a placeholder.**

The actual web app (Vite + React, route groups, i18n, design system) is delivered by **ARC-004** (per story backlog entry ARC-004 and the web-frontend blueprint in `3-architecture/3.2-blueprints/web-frontend-blueprint.md`).

This placeholder exists so that the monorepo workspace declared in `pnpm-workspace.yaml` resolves cleanly during `pnpm install`. Do not add application code here until ARC-004 lands.
