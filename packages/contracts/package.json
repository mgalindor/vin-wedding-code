{
  "name": "@wendy/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Wendy Planner \u2014 shared contracts package (DTOs + ID types per ADR-13, ADR-14).",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",

  "scripts": {
    "lint": "eslint \"src/**/*.ts\" \"test/**/*.ts\"",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest"
  },
  "dependencies": {
    "nanoid": "^5.0.9"
  },
  "devDependencies": {
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.1",
    "reflect-metadata": "^0.2.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
